# Server Configuration
PORT=5004
NODE_ENV=development
# Set to 'production' when deploying live

# Frontend URL (For CORS) - Optional but recommended
FRONTEND_URL=http://localhost:5173

# Database Connection (Neon.tech)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Authentication (One single strong key)
JWT_SECRET=sjbnfjsdbfjfbja2649182748213

# External APIs
GNEWS_API_KEY=your_gnews_api_key

# --- Azure AI Services ---
AZURE_OPENAI_KEY=your_azure_openai_api_key

# Chat Model (gpt-4o-mini / gpt-35-turbo)
AZURE_OPENAI_ENDPOINT=your_chat_model_endpoint_url
AZURE_OPENAI_CHAT_DEPLOYMENT=your_chat_deployment_name

# Embedding Model (text-embedding-ada-002)
AZURE_OPENAI_EMBEDDING_ENDPOINT=your_embedding_model_endpoint_url
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=your_embedding_deployment_name

# DALL-E 3 (Image Generation)
AZURE_OPENAI_DALLE_ENDPOINT=your_dalle3_endpoint_url
AZURE_OPENAI_DALLE_DEPLOYMENT=dall-e-3